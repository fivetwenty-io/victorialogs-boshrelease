#!/bin/bash
set -e

PORT=<%= p('vlselect.port') %>

# Check if VLSelect is responding to health checks
if curl -sf "http://localhost:${PORT}/health" >/dev/null 2>&1; then
    exit 0
else
    echo "VLSelect health check failed"
    exit 1
fi
