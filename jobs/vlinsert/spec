---
name: vlinsert
packages:
- victoria-logs

templates:
  bpm.yml: config/bpm.yml
  pre-start: bin/pre-start
  post-deploy: bin/post-deploy
  post-start: bin/post-start
  vlinsert: bin/vlinsert
  vlinsert-health-check: bin/vlinsert-health-check

consumes:
- name: vlstorage
  type: vlstorage

provides:
- name: vlinsert
  type: vlinsert
  properties:
  - vlinsert.http_port

properties:
  # Core HTTP settings
  vlinsert.http_listen_addr:
    description: "TCP address to listen for incoming HTTP requests"
    default: ":9481"
  vlinsert.http_port:
    description: "Port for VictoriaLogs insert HTTP API"
    default: 9481
  vlinsert.http_listen_addr_use_proxy_protocol:
    description: "Whether to use proxy protocol for connections"
    default: false
  vlinsert.http_path_prefix:
    description: "Optional prefix to add to all paths handled by http server"
    default: ""
  vlinsert.http_conn_timeout:
    description: "Incoming connection timeout duration"
    default: "2m"
  vlinsert.http_idle_conn_timeout:
    description: "Timeout for incoming idle http connections"
    default: "1m"
  vlinsert.http_max_graceful_shutdown_duration:
    description: "Maximum duration for graceful HTTP server shutdown"
    default: "7s"
  vlinsert.http_shutdown_delay:
    description: "Optional delay before http server shutdown"
    default: ""
  vlinsert.http_disable_response_compression:
    description: "Disable compression of HTTP responses"
    default: false
  vlinsert.http_disable_keepalive:
    description: "Disable HTTP keep-alive for incoming connections"
    default: false
  vlinsert.http_disable_cors:
    description: "Disable CORS for all origins"
    default: false

  # HTTP Security headers
  vlinsert.http_header_csp:
    description: "Value for Content-Security-Policy header"
    default: ""
  vlinsert.http_header_frame_options:
    description: "Value for X-Frame-Options header"
    default: ""
  vlinsert.http_header_hsts:
    description: "Value for Strict-Transport-Security header"
    default: ""

  # Authentication
  vlinsert.http_auth_username:
    description: "Username for HTTP server's Basic Auth"
    default: ""
  vlinsert.http_auth_password:
    description: "Password for HTTP server's Basic Auth"
    default: ""
  vlinsert.metrics_auth_key:
    description: "Auth key for /metrics endpoint"
    default: ""
  vlinsert.flags_auth_key:
    description: "Auth key for /flags endpoint"
    default: ""
  vlinsert.pprof_auth_key:
    description: "Auth key for /debug/pprof/* endpoints"
    default: ""

  # Core insert settings
  vlinsert.max_concurrent_inserts:
    description: "Maximum number of concurrent insert requests"
    default: 32
  vlinsert.insert_max_queue_duration:
    description: "Maximum duration to wait in queue when max concurrent inserts are executed"
    default: "1m"
  vlinsert.insert_max_line_size_bytes:
    description: "Maximum size of a single log line"
    default: 262144
  vlinsert.insert_max_fields_per_line:
    description: "Maximum number of fields per log line"
    default: 1000
  vlinsert.insert_max_request_size:
    description: "Maximum size in bytes of a single insert request"
    default: 67108864
  vlinsert.max_insert_request_size:
    description: "Maximum size in bytes of a single insert request (alias for insert_max_request_size)"
    default: 67108864

  # Log ingestion settings
  vlinsert.log_ingested_rows:
    description: "Log all ingested log entries"
    default: false
  vlinsert.log_new_streams:
    description: "Log creation of new streams"
    default: false
  vlinsert.default_msg_value:
    description: "Default value for _msg field if missing"
    default: "missing _msg field"

  # Syslog settings
  vlinsert.syslog_listen_addr_tcp:
    description: "TCP addresses to listen for Syslog messages"
    default: []
  vlinsert.syslog_listen_addr_udp:
    description: "UDP addresses to listen for Syslog messages"
    default: []
  vlinsert.syslog_listen_addr_unix:
    description: "Unix socket paths to listen for Syslog messages"
    default: []
  vlinsert.syslog_timezone:
    description: "Timezone for parsing RFC3164 syslog timestamps"
    default: "Local"
  vlinsert.syslog_use_local_timestamp_tcp:
    description: "Use local timestamp instead of original for TCP syslog"
    default: []
  vlinsert.syslog_use_local_timestamp_udp:
    description: "Use local timestamp instead of original for UDP syslog"
    default: []
  vlinsert.syslog_stream_fields_tcp:
    description: "Fields to use as log stream labels for TCP syslog"
    default: []
  vlinsert.syslog_stream_fields_udp:
    description: "Fields to use as log stream labels for UDP syslog"
    default: []
  vlinsert.syslog_ignore_fields_tcp:
    description: "Fields to ignore for TCP syslog"
    default: []
  vlinsert.syslog_ignore_fields_udp:
    description: "Fields to ignore for UDP syslog"
    default: []

  # DataDog settings
  vlinsert.datadog_max_request_size:
    description: "Maximum size for DataDog requests"
    default: 67108864
  vlinsert.datadog_stream_fields:
    description: "Fields to use as stream fields for DataDog logs"
    default: []
  vlinsert.datadog_ignore_fields:
    description: "Fields to ignore for DataDog logs"
    default: []

  # OpenTelemetry settings
  vlinsert.opentelemetry_max_request_size:
    description: "Maximum size for OpenTelemetry requests"
    default: 67108864

  # Loki settings
  vlinsert.loki_max_request_size:
    description: "Maximum size for Loki requests"
    default: 67108864
  vlinsert.loki_disable_message_parsing:
    description: "Disable automatic parsing of JSON in Loki messages"
    default: false

  # Memory settings
  vlinsert.memory_allowed_percent:
    description: "Allowed percent of system memory VictoriaLogs caches may occupy"
    default: 60
  vlinsert.memory_allowed_bytes:
    description: "Allowed size of system memory VictoriaLogs caches may occupy"
    default: 0

  # String interning settings
  vlinsert.intern_string_max_len:
    description: "Maximum length for strings to intern"
    default: 500
  vlinsert.intern_string_cache_expire_duration:
    description: "Expiry duration for interned strings cache"
    default: "6m"
  vlinsert.intern_string_disable_cache:
    description: "Disable caches for interned strings"
    default: false

  # Insert concurrency settings
  vlinsert.insert_concurrency:
    description: "Average number of concurrent data ingestion requests per storage node"
    default: 2
  vlinsert.insert_disable_compression:
    description: "Disable compression when sending data to storage nodes"
    default: false

  # Logging settings
  vlinsert.logger_level:
    description: "Minimum level of errors to log (INFO, WARN, ERROR, FATAL, PANIC)"
    default: "INFO"
  vlinsert.logger_format:
    description: "Format for logs (default, json)"
    default: "default"
  vlinsert.logger_output:
    description: "Output for logs (stderr, stdout)"
    default: "stderr"
  vlinsert.logger_timezone:
    description: "Timezone for log timestamps"
    default: "UTC"
  vlinsert.logger_max_arg_len:
    description: "Maximum length of a single logged argument"
    default: 5000
  vlinsert.logger_disable_timestamps:
    description: "Disable timestamps in logs"
    default: false
  vlinsert.logger_json_fields:
    description: "Rename fields in JSON formatted logs"
    default: ""
  vlinsert.logger_errors_per_second_limit:
    description: "Per-second limit on ERROR messages"
    default: 0
  vlinsert.logger_warns_per_second_limit:
    description: "Per-second limit on WARN messages"
    default: 0

  # Metrics exposure
  vlinsert.metrics_expose_metadata:
    description: "Expose TYPE and HELP metadata at /metrics"
    default: false

  # Push metrics settings
  vlinsert.pushmetrics_url:
    description: "URLs to push metrics to"
    default: []
  vlinsert.pushmetrics_interval:
    description: "Interval for pushing metrics"
    default: "10s"
  vlinsert.pushmetrics_extra_label:
    description: "Extra labels for pushed metrics"
    default: []
  vlinsert.pushmetrics_header:
    description: "HTTP headers for push requests"
    default: []
  vlinsert.pushmetrics_disable_compression:
    description: "Disable compression for push requests"
    default: false

  # TLS Configuration
  vlinsert.tls:
    description: "Enable TLS for incoming HTTP requests"
    default: []
  vlinsert.tls_cert_file:
    description: "Path to TLS certificate file"
    default: []
  vlinsert.tls_key_file:
    description: "Path to TLS key file"
    default: []
  vlinsert.tls_cipher_suites:
    description: "TLS cipher suites"
    default: []
  vlinsert.tls_min_version:
    description: "Minimum TLS version"
    default: []
  vlinsert.mtls:
    description: "Require valid client certificate"
    default: []
  vlinsert.mtls_ca_file:
    description: "Path to TLS Root CA for client verification"
    default: []

  # Environment flags
  vlinsert.envflag_enable:
    description: "Enable reading flags from environment variables"
    default: false
  vlinsert.envflag_prefix:
    description: "Prefix for environment variables"
    default: ""

  # IPv6
  vlinsert.enable_tcp6:
    description: "Enable IPv6 for listening and dialing"
    default: false

  # File system settings
  vlinsert.fs_disable_mmap:
    description: "Use pread() instead of mmap()"
    default: false
  vlinsert.filestream_disable_fadvise:
    description: "Disable fadvise() syscall"
    default: false

  # License Configuration
  vlinsert.license:
    description: "License key for VictoriaMetrics Enterprise"
    default: ""
  vlinsert.license_file:
    description: "Path to license file"
    default: ""
  vlinsert.license_file_reload_interval:
    description: "Interval for reloading license file"
    default: "1h"
  vlinsert.license_force_offline:
    description: "Enable offline license verification"
    default: false

  # TLS Autocert Configuration
  vlinsert.tls_autocert_hosts:
    description: "Hostnames for Let's Encrypt certificates"
    default: []
  vlinsert.tls_autocert_email:
    description: "Contact email for Let's Encrypt"
    default: ""
  vlinsert.tls_autocert_cache_dir:
    description: "Directory for Let's Encrypt certificates"
    default: ""

  # Replication and Routing
  vlinsert.replication_factor:
    description: "Replication factor for data"
    default: 1
  vlinsert.disable_rerouting:
    description: "Disable automatic rerouting during unavailability"
    default: false
  vlinsert.disable_rerouting_on_unavailable:
    description: "Disable rerouting when storage nodes are unavailable"
    default: false
  vlinsert.drop_samples_on_overload:
    description: "Drop samples instead of queueing during overload"
    default: false

  # Storage Node Settings
  vlinsert.storage_node_discovery_interval:
    description: "Interval for discovering storage nodes"
    default: "2s"
  vlinsert.storage_node_filter:
    description: "Filter for storage node discovery"
    default: ""
  vlinsert.vmstorage_dial_timeout:
    description: "Timeout for dialing storage nodes"
    default: "5s"
  vlinsert.vmstorage_user_timeout:
    description: "User timeout for storage node connections"
    default: "0s"

  # RPC Settings
  vlinsert.rpc_disable_compression:
    description: "Disable RPC compression"
    default: false
  vlinsert.rpc_handshake_timeout:
    description: "RPC handshake timeout"
    default: "3s"

  # Cache Settings
  vlinsert.cache_expire_duration:
    description: "Duration before cache entries expire"
    default: "30m"
  vlinsert.prev_cache_removal_percent:
    description: "Percentage of previous cache to remove"
    default: 0.1
  vlinsert.blockcache_misses_before_caching:
    description: "Number of misses before caching a block"
    default: 2

  # Label Settings
  vlinsert.sort_labels:
    description: "Sort labels in metrics"
    default: false
  vlinsert.max_labels_per_timeseries:
    description: "Maximum labels per timeseries"
    default: 30
  vlinsert.max_label_name_len:
    description: "Maximum label name length"
    default: 256
  vlinsert.max_label_value_len:
    description: "Maximum label value length"
    default: 16384

  # Cluster Native Settings
  vlinsert.cluster_native_listen_addr:
    description: "Address for cluster native protocol"
    default: ""
  vlinsert.cluster_native_vminsert_conns_shutdown_duration:
    description: "Duration for shutting down vminsert connections"
    default: "25s"

  # Cluster TLS Settings
  vlinsert.cluster_tls:
    description: "Enable TLS for cluster communication"
    default: false
  vlinsert.cluster_tls_ca_file:
    description: "CA file for cluster TLS"
    default: ""
  vlinsert.cluster_tls_cert_file:
    description: "Certificate file for cluster TLS"
    default: ""
  vlinsert.cluster_tls_key_file:
    description: "Key file for cluster TLS"
    default: ""
  vlinsert.cluster_tls_insecure_skip_verify:
    description: "Skip TLS verification for cluster"
    default: false

  # Graphite Protocol Settings
  vlinsert.graphite_listen_addr:
    description: "Address to listen for Graphite protocol"
    default: ""
  vlinsert.graphite_listen_addr_use_proxy_protocol:
    description: "Use proxy protocol for Graphite"
    default: false
  vlinsert.graphite_trim_timestamp:
    description: "Trim timestamp precision for Graphite"
    default: "1s"
  vlinsert.graphite_sanitize_metric_name:
    description: "Sanitize Graphite metric names"
    default: false

  # InfluxDB Protocol Settings
  vlinsert.influx_listen_addr:
    description: "Address to listen for InfluxDB protocol"
    default: ""
  vlinsert.influx_listen_addr_use_proxy_protocol:
    description: "Use proxy protocol for InfluxDB"
    default: false
  vlinsert.influx_database_names:
    description: "InfluxDB database names to accept"
    default: []
  vlinsert.influxdb_label:
    description: "Label for InfluxDB database"
    default: "db"
  vlinsert.influx_measurement_field_separator:
    description: "Separator between measurement and field"
    default: "_"
  vlinsert.influx_skip_measurement:
    description: "Skip measurement name in labels"
    default: false
  vlinsert.influx_skip_single_field:
    description: "Skip field name for single field"
    default: false
  vlinsert.influx_trim_timestamp:
    description: "Trim timestamp precision for InfluxDB"
    default: "1ms"
  vlinsert.influx_max_line_size:
    description: "Maximum line size for InfluxDB"
    default: 262144
  vlinsert.influx_max_request_size:
    description: "Maximum request size for InfluxDB"
    default: 67108864
  vlinsert.influx_force_stream_mode:
    description: "Force stream mode for InfluxDB"
    default: false

  # OpenTSDB Protocol Settings
  vlinsert.opentsdb_listen_addr:
    description: "Address to listen for OpenTSDB protocol"
    default: ""
  vlinsert.opentsdb_listen_addr_use_proxy_protocol:
    description: "Use proxy protocol for OpenTSDB"
    default: false
  vlinsert.opentsdb_http_listen_addr:
    description: "HTTP address for OpenTSDB protocol"
    default: ""
  vlinsert.opentsdb_http_listen_addr_use_proxy_protocol:
    description: "Use proxy protocol for OpenTSDB HTTP"
    default: false
  vlinsert.opentsdb_trim_timestamp:
    description: "Trim timestamp precision for OpenTSDB"
    default: "1s"
  vlinsert.opentsdb_http_trim_timestamp:
    description: "Trim timestamp precision for OpenTSDB HTTP"
    default: "1ms"
  vlinsert.opentsdb_http_max_insert_request_size:
    description: "Maximum insert request size for OpenTSDB HTTP"
    default: 33554432

  # CSV Import Settings
  vlinsert.csv_trim_timestamp:
    description: "Trim timestamp precision for CSV"
    default: "1ms"

  # DataDog Protocol Settings
  vlinsert.datadog_max_insert_request_size:
    description: "Maximum insert request size for DataDog"
    default: 67108864
  vlinsert.datadog_sanitize_metric_name:
    description: "Sanitize DataDog metric names"
    default: false

  # NewRelic Protocol Settings
  vlinsert.newrelic_max_insert_request_size:
    description: "Maximum insert request size for NewRelic"
    default: 67108864

  # OpenTelemetry Protocol Settings
  vlinsert.opentelemetry_use_prometheus_naming:
    description: "Use Prometheus naming for OpenTelemetry"
    default: false

  # Import Settings
  vlinsert.import_max_line_len:
    description: "Maximum line length for imports"
    default: 10485760

  # Relabeling Settings
  vlinsert.relabel_config:
    description: "Path to relabel config file"
    default: ""
  vlinsert.relabel_config_check_interval:
    description: "Interval to check relabel config"
    default: ""

  # Metadata Settings
  vlinsert.enable_metadata:
    description: "Enable metadata endpoints"
    default: false

  # Query Settings
  vlinsert.search_deny_partial_response:
    description: "Deny partial responses in search"
    default: false
  vlinsert.deny_query_tracing:
    description: "Deny query tracing"
    default: false

  # Prometheus Compatibility
  vlinsert.use_prom_compatible_naming:
    description: "Use Prometheus compatible naming"
    default: false