#!/bin/bash
set -e

HTTP_PORT=<%= p('vlstorage.http_port') %>

# Check if vmstorage is responding to health checks
if curl -sf "http://localhost:${HTTP_PORT}/health" >/dev/null 2>&1; then
    exit 0
else
    echo "vmstorage health check failed"
    exit 1
fi
