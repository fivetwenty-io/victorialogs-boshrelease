#!/bin/bash
set -e

PORT=<%= p('vlstorage.port') %>

# Check if VLStorage is responding to health checks
if curl -sf "http://localhost:${PORT}/health" >/dev/null 2>&1; then
    exit 0
else
    echo "VLStorage health check failed"
    exit 1
fi
